import { ActiveBlockedVivoPipe } from './active-blocked-vivo.pipe';

describe('ActiveBlockedVivoPipe', () => {
  it('create an instance', () => {
    const pipe = new ActiveBlockedVivoPipe();
    expect(pipe).toBeTruthy();
  });
});
