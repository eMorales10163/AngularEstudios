import { ActiveBlockedPipe } from './active-blocked.pipe';

describe('ActiveBlockedPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ActiveBlockedPipe();
    expect(pipe).toBeTruthy();
  });
});
